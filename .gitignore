# ---------------------------------------------------------------------------
# 실행 중 생성되는 파일 — 절대 커밋하지 말 것
#
# browser_profiles 에는 배틀넷 로그인 세션 쿠키가 들어간다. 나머지 둘은 코드 원본을
# 담지 않도록 만들어져 있지만(로그는 마스킹, 기록은 단방향 해시), 개인 사용 흔적이므로
# 저장소에 올릴 이유가 없다.
# ---------------------------------------------------------------------------
browser_profiles/
run_log.txt
registered_codes.txt
results.csv

# ---------------------------------------------------------------------------
# 빌드 산출물 — 배포본은 GitHub Releases에 첨부하고 저장소에는 두지 않는다.
# _internal 만 124MB가 넘는다.
# ---------------------------------------------------------------------------
EzDrops.exe
_internal/
build/
dist/
_build_tmp/
_dist_tmp/

# ---------------------------------------------------------------------------
# 파이썬
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ---------------------------------------------------------------------------
# 에디터 / OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
