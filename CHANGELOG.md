# EzDrops 변경 이력

모든 항목은 실제로 겪은 문제와 그 원인, 검증 방법까지 남긴 개발 기록입니다.
같은 증상을 다시 만났을 때 이미 시도해본 것을 반복하지 않으려는 목적이 큽니다.

최신 버전이 위에 옵니다.

---

### 1.9.19

코드 전체를 담던 `results.csv`를 없애고, 되돌릴 수 없는 SHA-256 지문만 쌓는 중복 등록 방지 기록(`registered_codes.txt`)으로 대체. 시작할 때 입력 코드의 지문을 기록과 대조해 "이미 등록한 코드"를 알려주고, 빼고 진행할지 그대로 진행할지 고르게 한다. GUI 체크박스로 끌 수 있다(기본 켜짐).

원래 `results.csv`는 `"w"` 모드로 매번 덮어써서 직전 한 배치만 남았고, 중복 제거 로직(`start_redemption`의 `seen` 집합)은 이 파일을 읽지도 않았다 — 즉 "중복 확인"이라는 용도로는 애초에 동작하지 않는 파일이었다. 반면 실행 간 중복을 확인하려면 등록 이력을 계속 쌓아야 하는데, 그건 v1.9.18에서 줄이려던 "코드가 디스크에 남는 문제"와 정면으로 부딪힌다. 코드 대신 지문을 저장하는 방식으로 이 충돌을 없앴다. 설치마다 다른 salt를 섞어 해시하므로 미리 계산해둔 표로 대조하는 공격도 성립하지 않는다.

자동으로 제외하지 않고 사용자에게 묻는 이유는, v1.9.0 이후 이 프로그램이 확인 버튼 클릭 여부만으로 성공을 판정하기 때문 — 기록이 실제 등록 결과와 다를 수 있어 최종 판단을 사용자에게 남긴다.

→ 검증: 기록 파일에 코드 원본이(앞 8자까지 포함해) 남지 않는지, 그럼에도 중복이 잡히는지, 누적 저장·salt 재사용·설치별 salt 분리·손상된 파일 복원력까지 14건 전부 통과. **레이아웃 회귀 발견 및 수정**: 설명 문구를 `login_frame` 그리드 안에 넣었더니 열 너비가 문구 길이만큼 늘어나 비밀번호 입력칸이 창 밖으로 밀려 잘렸다 — 창 캡처로 확인하고 설명을 프레임 바깥 독립된 줄로 분리해 해결.

### 1.9.18

오픈소스 공개를 앞두고 로그에서 리딤 코드를 부분 마스킹. `run_log.txt`는 "문제가 생기면 이 파일을 그대로 보내면 된다"는 전제로 만든 파일이라, 코드가 통째로 찍히면 그 파일 하나로 코드가 유출된다. 화면 로그도 캡처해 공유되는 일이 잦아 동일하게 적용. 코드를 아예 빼는 대신 앞 4자 + 글자 수(`ABCD***(17자)`)만 남겼는데, 어느 코드가 실패했는지 구분이 안 되면 로그의 존재 이유가 사라지기 때문이다. 글자 수를 남기는 건 이 프로그램에서 실제로 겪은 버그 상당수가 붙여넣기 과정에서 코드가 잘리거나 이어붙는 문제였어서, 길이만 봐도 그 유형인지 판단할 수 있기 때문. 더불어 로그로 나가는 예외 메시지는 전부 `scrub_codes()`로 한 번 거른다 — Playwright 오류 메시지가 입력값을 포함하는지는 버전에 따라 달라질 수 있어, 포함되지 않는다고 추정하지 않고 무조건 걸러내는 쪽을 택했다. → 검증: `mask_code`/`scrub_codes` 단위 케이스 11건, 소스 정적 검사(마스킹·세척 없이 코드나 예외를 남기는 `self.log()` 호출이 있는지 전수 스캔), 실제 Tk mainloop을 돌려 `run_log.txt` 파일에 코드 원본이 남지 않는지 확인까지 전부 통과. 정적 검사에서 예외를 그대로 남기던 5곳이 추가로 발견되어 같이 수정. **주의: `results.csv`에는 결과 기록 용도로 코드 전체가 그대로 저장된다.**

### 1.9.17

창별 자동 로그인 간격을 GUI에서 직접 조절할 수 있게 함(Spinbox, 기본 5초, 0~60초). v1.6.4~1.9.4에서 2초→10초→3초→5초로 계속 바꿔온 값인데, 배틀넷 인증기 앱 승인에 걸리는 시간은 그때그때 다르므로 코드에 박아두는 것 자체가 맞지 않았음. 0을 넣으면 시차 없이 전부 동시에 로그인한다. 입력이 숫자가 아니거나 범위를 벗어나면 조용히 넘어가지 않고 로그로 알린 뒤 기본값/경계값으로 보정하고, 보정된 값을 입력칸에도 되돌려 써서 화면 표시와 실제 동작이 어긋나지 않게 한다. 실제 적용된 간격은 run_log.txt에도 남긴다.

### 1.9.16

배포 폴더 정리 — v1.9.15에서 아이콘을 크기별 PNG 7개로 쪼개면서 그 파일들이 exe 옆에 그대로 흩어져 배포 폴더가 지저분해졌음. 읽기 전용 리소스용 경로(get_resource_dir)를 따로 만들어 아이콘을 _internal 안(sys._MEIPASS)에서 읽도록 옮김 — 이제 배포 폴더 루트에는 EzDrops.exe와 _internal 폴더만 남는다. 쓰기가 필요한 run_log.txt와 browser_profiles는 사용자가 바로 찾을 수 있어야 하므로 종전대로 exe 옆(get_base_dir)에 그대로 둔다. 소스와 아이콘 원본도 src\ 하위로 분리.

### 1.9.15

**진짜 원인 확정 #2.** v1.9.14로도 안 바뀐다는 재지적 — 화면 캡처로 확인하려다 다른 창 내용이 섞여 들어올 위험이 있어 중단하고, 대신 WM_GETICON으로 창이 실제로 들고 있는 아이콘 핸들을 직접 조회하는 방식으로 전환. 확인해보니 `iconbitmap()`은 다중 해상도 .ico를 줘도 프레임 하나만 가져다 자체적으로(저품질로) 늘리거나 줄여쓰고 있었음 — 파일이나 캐시 문제가 아니라 이 API 자체의 한계였음. `icon_16/24/32/48/64/128/256.png` 개별 파일을 만들어 `root.iconphoto(True, *images)`로 교체 — Tk가 각 상황에 맞는 크기를 그대로 골라 씀. WM_GETICON으로 재조회해 16px/32px 핸들이 각각 우리가 만든 원본 그대로(추가 흐림 없이) 쓰이는 것까지 확인.

### 1.9.14

v1.9.13(캐시 삭제까지 포함)으로도 여전히 안 바뀐다는 재지적 — v1.9.11부터 PNG 압축 프레임을 직접 담는 ICO를 손으로 구성했는데, 이 방식이 Tk/Windows 타일 렌더링 경로와 호환이 안 됐을 가능성이 있어 Pillow의 표준 ICO 라이터(작은 크기는 검증된 classic BMP 프레임)로 되돌림. 캔버스가 254px라 256 프레임이 큰 쪽으로 스케일이 안 돼 조용히 누락되던 버그도 같이 발견해 캔버스 최소 256 보장으로 수정. 캔버스 전체에 가볍게 언샤프 마스크 한 번만 적용(사이즈별 개별 처리 대신 단순화).

### 1.9.13

**진짜 원인 확정.** v1.9.12로도 실제 작업표시줄에서 흐리다는 재지적 — 실제로 exe를 띄워 화면을 캡처해서 확인해보니 진짜로 흐렸음. DPI는 100%로 배율 문제는 아니었고, Windows가 작업표시줄/타이틀바에서 요청하는 실제 물리 픽셀 크기(예: 24px)가 그동안의 .ico(16/32/48/64/128/256)엔 없어서, 없는 크기를 요청받으면 다른 프레임을 억지로 늘리거나 줄여써서 흐려지는 것으로 확인. 16/20/24/28/32/40/48/64/96/128/256으로 훨씬 촘촘하게 채워 항상 정확히 일치하는 프레임이 있게 함.

### 1.9.12

그래도 여전히 흐리다는 재지적 — 사용자가 직접 윤곽선을 굵게 다시 그린 256×256 원본(`EzDrops 256.png`)을 제공. 이 원본으로 icon.ico 재생성(동일하게 16/32px만 언샤프 마스크 보강). 8배 확대 비교 결과 32/48px는 윤곽선이 뚜렷하게 살아남고 이목구비도 잘 구분됨.

### 1.9.11

작업표시줄/창 아이콘이 흐리다는 지적 — 16/32/48px로 축소하면 원본의 가는 선·그라데이션 디테일이 뭉개지는 게 원인(기술적 결함이 아니라 그림 자체가 작은 크기엔 디테일이 많음). Pillow의 기본 ICO 저장은 사이즈별 후처리가 불가능해서, PNG 압축 프레임을 직접 담는 ICO를 손으로 구성하도록 변경 — 16/32/48px에만 언샤프 마스크로 윤곽선 대비를 보강. 8배 확대 미리보기로 보정 전/후 비교, 32·48px는 뚜렷하게 개선 확인(16px는 원래 한계가 있어 개선은 되지만 여전히 작음).

### 1.9.10

사용자가 아이콘 원본 이미지를 수정(`EzDrops.png`)해서 icon.ico를 재생성. PyInstaller가 main.py 변경이 없어 EXE 재빌드를 건너뛰는 바람에 새 아이콘이 실제로는 반영이 안 됐던 걸 발견 — `--clean`으로 강제 재빌드해서 해결. exe에 실제로 새 아이콘이 박혔는지 System.Drawing으로 아이콘을 추출해 시각적으로 재확인.

### 1.9.9

앱 아이콘(파비콘)을 사용자가 제공한 웃는 물방울 이미지로 교체. 투명 배경 원본에서 여백을 잘라내고 정사각 캔버스에 중앙 정렬해 16~256px 다중 해상도 .ico로 변환 (`icon.ico`), 창 아이콘(`root.iconbitmap`)과 exe 자체 아이콘(PyInstaller --icon) 둘 다에 적용.

### 1.9.8

앱을 닫아도 크롬 프로세스/백그라운드 작업이 남아있을 수 있다는 지적 — 종료 시 열려있는 Playwright 컨텍스트를 전부 명시적으로 닫도록 수정. asyncio.run() 대신 백그라운드 스레드가 자기 루프를 직접 관리하게 바꿔(run_async_worker) 앱 종료 시점(on_close)에 그 루프로 run_coroutine_threadsafe를 통해 "컨텍스트 전부 닫기"를 즉시 예약·대기할 수 있게 함. 컨텍스트가 닫히면 백그라운드 파이프라인도 스스로 정리되므로 워커 스레드 종료까지 짧게 기다린 뒤 창을 닫는다. 별도 스레드+이벤트 루프로 축소 재현해 실제로 다른 스레드의 루프에 있는 컨텍스트들이 정상적으로 닫히고 워커 스레드도 뒤따라 종료되는 것까지 확인.

### 1.9.7

**버그 수정.** "지금 전체 확인 버튼 동시 클릭"을 기다리는 동안 브라우저를 전부 닫거나 입력창의 코드를 전부 지워도 이를 감지할 방법이 없어 무한 대기에 빠지고, "시작"/"코드 재등록" 버튼이 영원히 잠긴 채로 남는 버그를 발견해 수정. 창마다 닫힘 이벤트를 배치 시작 시점부터 등록해두고(close_events), 입력창엔 <<Modified>> 이벤트로 "전부 지워짐"을 감지하는 abandon_trigger를 추가 — 확인 대기 루프가 이 둘도 함께 감시하다가 감지되면 포기하고 정상 종료 경로로 빠져 버튼을 되돌린다. 실제 mainloop로 abandon_trigger 감지 로직(부분 삭제엔 반응 안 함, 전체 삭제에만 반응, 재사용 사이클에도 재발동) 재현 테스트 통과 확인.

### 1.9.6

성공한 코드가 삭제 전 아무 안내 없이 바로 사라져서 헷갈린다는 요청 — 즉시 삭제 대신 "N초 후 삭제..." 마커를 1초마다 5→1까지 갱신해 보여준 뒤 0초에 그 줄을 지우도록 변경 (`_countdown_delete`). 실제 mainloop로 카운트다운 중간 상태·최종 삭제·실패 코드와의 동시 처리까지 재현 테스트 통과 확인.

### 1.9.5

(1) 앱 종료 시 저장된 로그인 세션(browser_profiles)을 전부 삭제해서 다음 실행 땐 항상 재로그인하게 함. (2) 코드 등록이 성공하면 입력창에서 그 줄을 표시만 하지 않고 아예 삭제, 실패한 코드만 마커를 남겨 재등록 대상이 눈에 띄게 함. 실제 Tkinter mainloop로 재현 테스트(성공/실패/미처리 혼합, 전부 성공 시 입력창이 완전히 비는지까지) 통과 확인.

### 1.9.4

창별 자동 로그인 시도 간격(배틀넷 인증기 앱 승인 시간 확보용)을 3초 → 5초로 조정.

### 1.9.3

줄바꿈된 다음 줄이 이전 줄과 안 겹치고 바로 붙어서 뜨던 걸, 이전 줄 창의 하단 1/3 지점부터 겹치도록(세로 오프셋 = 창 높이의 2/3) 변경. CDP로 실제 좌표 검증(1행: y=0, 2행: y=480 — 720의 정확히 2/3, 즉 이전 창 하단 1/3만 겹침).

### 1.9.2

공식 이름을 EzDrops로 확정 — 창 제목, 클래스명(EzDropsApp), 빌드 산출물 이름 (EzDrops.exe)을 전부 갱신.

### 1.9.1

exe 용량(560MB)이 대부분 번들 크로미움(약 430MB) 때문 — 사용자가 Edge 대신 사람들이 더 많이 쓰는 크롬 기준으로 줄여달라고 요청. 번들 크로미움을 빼고 PC에 이미 설치된 구글 크롬을 그대로 쓰도록 `channel="chrome"` 적용. exe 용량이 약 560MB → 130MB로 감소. 단, 대상 PC에 크롬이 설치돼 있어야 동작함(예전엔 크롬 없어도 됐음).

### 1.9.0

**대폭 단순화.** "확인 버튼을 누르면 정상 등록되는 게 당연하니 따로 결과를 확인할 필요 없다"는 요청 — 확인 클릭 후 결과 화면 텍스트를 읽어 성공/실패/애매를 판정하고 애매하면 최대 2회 재제출하던 로직(parse_status/finalize_code/single_attempt 등)을 전부 삭제. 이제 "확인" 버튼 클릭 자체가 됐는지만 확인하고 바로 완료 처리한다. 부수 효과로, 결과 화면을 너무 일찍 읽어 빈 화면으로 오판해 이미 성공한 코드를 또 제출하던 버그(오늘 재현됨)도 근본적으로 사라짐. 순차 처리이던 결과 판정 단계가 없어지면서 배치 처리 속도도 훨씬 빨라짐(모든 탭이 동시 클릭 후 즉시 완료).

### 1.8.1

성공 화면("코드가 등록되었습니다")에서 재사용할 때, 그동안 goto()로 전체 페이지를 재이동시키던 걸 사용자 제보로 확인한 사이트 자체 "다른 코드 등록" 버튼 클릭으로 변경. 이 버튼이 없거나 클릭 후에도 입력란이 안 뜨면 기존 goto() 방식으로 폴백.

### 1.8.0

"실패로 표시된 코드만 다시 등록" 버튼(실패 코드만 필터링)을 삭제하고, 같은 자리에 "코드 재등록" 버튼으로 교체. "시작" 버튼과 동일하게 입력창의 코드를 그대로(필터링 없이) launch()로 보내서, v1.7.9의 "브라우저가 열려있으면 재사용" 기능을 그대로 탄다.

### 1.7.9

한 배치 등록이 끝나도 브라우저 창을 닫지 않고 대기하다가, "시작"을 다시 누르면 새로 창을 띄우지 않고 지금 열려있는(로그인도 이미 된) 창들에 새 코드를 넣어서 그대로 재사용하도록 변경. 코드 수가 더 많으면 부족한 만큼만 새 창을 추가로 연다.

### 1.7.8

"보기" 텍스트 버튼을 눈 모양 아이콘(👁)으로 교체하고, 테두리 있는 프레임으로 입력칸과 하나로 묶어서 비밀번호 칸 안쪽 오른쪽에 붙어있는 것처럼 보이게 변경.

### 1.7.7

토글이 아니라 "누르고 있는 동안만" 보이길 원한다는 정정 — 버튼을 누르는 순간만 마스킹 해제되고, 떼는 즉시 다시 * 로 돌아가도록 press/release 이벤트 기반으로 변경.

### 1.7.6

비밀번호 입력란 옆에 "보기" 버튼 추가 — 클릭 시 마스킹(*) 해제해서 내용 확인 가능, 다시 누르면 재마스킹. 실행 후 입력란이 비워질 때도 마스킹 상태를 * 로 자동 초기화.

### 1.7.5

3개마다 줄바꿈이 빠졌다는 지적 — v1.7.3 방식(줄 안 1/3 우측 이동 + 3개 넘으면 다음 줄로 아래 이동)으로 복원.

### 1.7.4

줄바꿈(계단식) 없이 세로 이동 없는 순수 우측 이동만 원한다는 피드백 — 모든 창이 y=0으로 고정되고 x만 창마다 1/3씩(약 426px) 증가하도록 단순화.

### 1.7.3

순수 3열 격자 대신, 줄 안에서는 1/3씩 우측 계단식 + 3개 넘으면 다음 줄(세로 전체 높이 이동)로 하이브리드 배치 변경. CDP로 5개 창 실제 좌표 재검증(0/426/852, 다음 줄 0/426).

### 1.7.2

대각선 계단식 배치를 요청대로 3열 격자 배치로 변경 — 가로 3개씩, 3개 넘으면 다음 줄로. CDP로 5개 창 실제 좌표까지 실측 검증(정확히 3열 격자로 배치됨).

### 1.7.1

실행 폴더에 browser_data_1, _2... 가 여러 개 흩어져 지저분해 보인다는 피드백 — 기능(세션 분리)은 유지하되, 전부 browser_profiles/slot_N 하위로 모아 겉보기를 정리.

### 1.7.0

**핵심 버그 수정.** run_log.txt 분석으로 확정 — 실제 성공 화면 문구가 "코드가 등록되었습니다"인데 성공 판정 키워드 목록에 이게 없어서, 이미 성공한 걸 "확인 필요"로 오판했음. 그 결과 불필요한 재시도 로직이 돌아가면서 **이미 성공한 코드를 또 제출**했고, 그래서 "이미 등록된 코드입니다"라는 응답을 받은 것 — 사이트는 정직했고 문제는 판정 로직이었음. 성공 키워드에 "등록되었습니다" 추가.

### 1.6.9

절반은 너무 넓게 펼쳐진다는 피드백으로 오프셋을 1/3(가로 약 427px, 세로 240px)로 축소.

### 1.6.8

40px는 너무 적다는 피드백 — 오프셋을 창 크기(뷰포트)의 절반(가로 640px, 세로 360px)으로 키워서, 이전 창이 절반 정도 보이는 계단식 배치로 변경.

### 1.6.7

브라우저 창이 전부 같은 위치에 겹쳐서 떠서 하나씩 확인하기 어렵다는 피드백 — 창마다 --window-position을 40px씩 대각선으로 어긋나게 줘서 계단식으로 배치되도록 수정. CDP로 실제 창 좌표까지 실측 검증(0,0 / 40,40 / 80,80으로 정확히 배치됨).

### 1.6.6

10초는 너무 길다는 사용자 피드백으로 3초로 재조정.

### 1.6.5

실제로는 SMS가 아니라 배틀넷 인증기 앱 승인(폰을 직접 들어서 눌러야 함)이었음 — 2초로는 턱없이 부족해 로그인 시도 간격을 10초로 상향.

### 1.6.4

run_log.txt로 실제 원인 확인 — 계정에 SMS 2FA가 걸려있어 자동 로그인이 비밀번호까지만 채우고 인증 단계에서 막힘. 이때 로그인 대기 단계에 탭별 보호가 없어서, 창 하나가 오류/닫힘이 나면 나머지 멀쩡한 창들까지 전부 같이 닫히던 치명적 버그를 수정(창별로 개별 처리). 또한 여러 창이 동시에 2FA를 요구하면 헷갈릴 수 있어, 자동 로그인 시도 자체도 창마다 2초씩 시차를 두도록 변경(사용자 요청).

### 1.6.3

창마다 로그인해야 하는 번거로움을 줄이기 위해 자동 로그인(선택 사항) 추가 — 이메일/비밀번호 입력란을 GUI에 추가하고, 배틀넷 로그인 2단계 폼(이메일 입력→계속→비밀번호 입력→로그인)을 자동으로 채운다. 비밀번호 입력란은 마스킹 처리, 입력값은 디스크나 로그 파일에 절대 저장되지 않고 해당 실행의 메모리에서만 사용된 뒤 즉시 입력란도 비움. 2FA/캡차 등으로 자동 로그인이 막히면 기존의 "코드 입력란 대기" 로직이 안전망이 되어 수동으로 마저 진행하면 된다.

### 1.6.2

진행 상황 로그를 화면뿐 아니라 실행 폴더의 run_log.txt 파일에도 그대로 저장하도록 추가. 문제가 생기면 캡처/복사 없이 이 파일을 그대로 전달하면 바로 분석 가능. 실행할 때마다 파일을 새로 시작해서 항상 가장 최근 실행 기록만 남는다.

### 1.6.1

세션이 독립됐으니 시차를 둘 이유가 없어졌다고 보고, 페이지 이동/코드 장전/등록 버튼 클릭 전부 시차 없이 완전 동시로 되돌림 (마지막 확인 클릭은 애초에 항상 동시였음).

### 1.6.0

**구조 변경.** 사용자가 직접 여러 브라우저를 따로 열어서 했을 땐 문제가 없었다는 점에 착안 — 지금까지는 탭만 여러 개였지 브라우저 프로필(세션/쿠키)은 하나를 공유하고 있었음. 코드마다 완전히 독립된 브라우저 프로필(browser_data_1, _2, ...)을 쓰도록 변경해, 서버가 "한 세션이 동시에 여러 건을 처리하려 한다"고 인식할 여지 자체를 없앰. 단, 세션이 분리되므로 로그인도 창마다 각각 필요(이후엔 창별로 자동 로그인 유지).

### 1.5.5

최종 "확인" 동시 클릭 정밀도 개선 — Playwright의 click()은 실제 클릭 전에 요소가 눌러도 되는 상태인지 확인하는 절차를 거치는데, 이 확인 시간이 탭마다 미세하게 달라 asyncio.gather로 동시에 시작해도 실제 클릭 순간이 탭마다 어긋날 수 있음. 버튼이 준비됐는지는 미리 확인해두고, 실제 클릭은 순수 JS(button.click())로 즉시 실행하도록 변경해 탭 간 클릭 타이밍 차이를 최소화. 로컬 테스트로 정상 동작 확인.

### 1.5.4

간격 기준 최종 확정 — "탭 10개는 3초, 그보다 적으면 같은 간격으로 더 빨리 끝난다"로 고정(3/9초 간격). "3초를 항상 채운다"는 이전 방식(1.5.1~1.5.3)을 폐기.

### 1.5.3

실제 실패 로그 분석으로 버그 2개 확인. (1) "처리 중" 화면이 /preload/ 주소가 아닌 다른 중간 주소에서도 뜨는데, 그 경우를 못 잡아서 불필요하게 재제출하거나 포기함 — 주소뿐 아니라 "잠시만 기다려주세요/처리 중입니다" 문구로도 판단하도록 확장. (2) 한 코드 처리 중 예외가 보호 없이 위로 튀면 브라우저 전체가 닫히고 그 여파로 무관한 다른 코드들까지 "context가 닫혔다"는 엉뚱한 에러로 같이 실패하던 치명적 버그 — 코드별 처리를 통째로 try/except로 감싸서 한 코드의 문제가 절대 다른 코드로 번지지 않도록 격리.

### 1.5.2

방향을 반대로 잘못 잡았던 걸 원위치 — 탭이 10개보다 적을 땐 더 빨리 끝나는 게 아니라 오히려 간격에 여유가 생겨야 한다는 사용자 의도 확인. 총 3초를 채우는 v1.5.1 방식(탭이 적을수록 간격이 넓어짐, 예: 5개면 0.75초 간격)으로 되돌림.

### 1.5.1

사용자가 원한 건 "탭당 3초 간격"이 아니라 "전체(최대 10개)를 3초 안에" 순차 처리였음 — 탭 개수와 무관하게 첫 클릭~마지막 클릭까지 총 3초 안에 끝나도록 간격을 재분배(10개 기준 약 0.33초 간격). [경고] 이미 0.4초 간격에서도 리디렉션 버그가 재현된 바 있어, 이보다 촘촘한 이 방식은 같은 버그가 재발할 가능성이 높다는 걸 알면서 사용자 요청으로 적용함.

### 1.5.0

사용자 요청으로 속도 절충안 적용 — 완전 순차(탭 완료까지 대기) 대신, 탭마다 "코드 등록" 클릭을 3초 간격으로만 시작하고 완료는 기다리지 않는 방식으로 변경. [주의] 이 방식은 1.4.9(완전 순차)만큼 검증되지 않았고, 사이트 처리가 3초보다 오래 걸리면 여전히 겹쳐서 리디렉션 버그가 재발할 위험이 있음 — 실사용 확인 필요.

### 1.4.9

**진짜 원인 확정 (새 코드로도 재현, 매번 정확히 탭 하나만 실패하는 패턴 확인).** 지금까지 페이지 이동(goto)만 순차로 고쳤는데, 정작 "코드 등록" 버튼 클릭 자체는 여전히 모든 탭이 동시에 누르고 있었음 — 이 클릭이 실제로 preload 페이지 이동을 유발하는 트리거라, 동시에 누르면 페이지 이동 때와 같은 부하로 리디렉션 초과가 재현됨. 개인 소유 코드는 서로 경쟁하지 않으므로, "코드 등록" 클릭~확인 화면 도달까지만 탭별로 완전히 순차 처리하도록 변경 (마지막 "확인" 클릭은 그대로 동시 유지 — 이미 정적인 화면이라 문제 없음이 실측으로 확인됨).

### 1.4.8

1.4.7로도 재현 — 타임아웃을 늘리는 방식은 타이밍이 어긋나면 또 재현될 수 있다고 보고, 구조적으로 막음: 결과가 애매할 때 현재 페이지가 아직 preload(처리 중) URL이면 같은 코드를 절대 재제출하지 않고 최대 60초 더 그냥 기다린다. preload가 아닌 다른 이유로 애매한 경우에만 실제 재제출(재시도)한다.

### 1.4.7

1.4.6으로도 같은 문제 재현 — "확인" 클릭 자체는 최대 60초 기다리게 늘렸지만, 클릭 "이후" 결과 화면이 뜨기까지 기다리는 시간(networkidle)은 10초로 그대로 남아있었음. 이것도 45초로 늘려서, 확인 클릭 후 결과가 늦게 뜨는 경우까지 커버하도록 수정.

### 1.4.6

사용자 화면 녹화 영상으로 실제 원인 확정. "코드 등록" 클릭 후 사이트가 /checkout/preload/<code 포함 토큰>으로 이동하는 로딩 화면이 30초 넘게 걸리는 걸 실측 — 기존 15~20초 타임아웃이 이걸 "실패"로 오판해서 같은 코드를 자동 재시도로 또 제출했고, 그게 리디렉션 초과 오류를 유발한 것으로 확인. 확인 화면 대기/클릭 타임아웃을 모두 60초로 늘려서 정상 처리 중인 걸 실패로 오판하지 않도록 수정.

### 1.4.5

프로필 초기화, 시차, 예외 정리까지 해도 로그인 후 코드 장전 단계에서 리디렉션 문제가 재현된다는 리포트 — 0.4초 시차로는 부족하다고 보고, 코드 장전 시 탭 재이동을 아예 완전히 순차(한 탭이 다 끝나야 다음 탭 시작)로 바꿈. 확인 클릭 단계는 그대로 동시 유지.

### 1.4.4

예외 발생 시 브라우저 창은 열린 채로 "시작" 버튼만 다시 눌리게 풀리던 버그 수정. 이 상태로 다시 시작을 누르면 같은 프로필로 브라우저를 중복 실행하게 되어, 크롬이 기존 창을 재활성화/새로고침하는 식으로 반응 — "브라우저가 계속 재시작되는" 것처럼 보이던 현상의 유력한 원인. 예외 시 브라우저를 확실히 닫은 뒤에만 버튼이 풀리도록 변경.

### 1.4.3

탭이 정확히 같은 순간에 동시에 checkout 페이지로 이동하면 "리디렉션 횟수 초과" (ERR_TOO_MANY_REDIRECTS) 에러가 나는 사용자 리포트 — flowTrackingId 같은 세션 공유 값을 여러 탭이 동시에 경합해서 생기는 것으로 추정, 탭마다 0.4초씩 시차를 둬서 회피. (실제 로그인 세션 없이는 재현 불가 — 근거 있는 추정에 기반한 수정, 실사용 확인 필요)

### 1.4.2

페이지 이동(goto) 두 곳이 try/except로 안 감싸져 있어서, 탭 하나가 "페이지가 작동하지 않습니다" 같은 연결 오류를 일으키면 그 예외가 전체 작업을 통째로 중단시키던 위험한 버그 수정 — 이제 탭별로 최대 2회 재시도하고, 그래도 실패하면 그 탭만 실패 처리하고 나머지 탭은 정상 진행. 로컬 테스트로 한 탭 실패가 나머지에 영향 없음을 확인.

### 1.4.1

위 변경으로 이미 로그인되어 코드 입력란이 보이는 탭까지 무조건 새로고침하는 바람에 불필요하게 깜박이던 문제 수정 — 이미 준비된 탭은 새로고침을 건너뛰도록 변경.

### 1.4.0

"1번 탭에서만 로그인" 제약 제거 — 세션은 모든 탭이 공유하는데 코드가 1번 탭만 지켜보고 있던 설계상 제약이었을 뿐이라, 아무 탭에서나 로그인하면 인식되도록 변경.

### 1.3.3

확인 버튼 동시 클릭 후 고정 4초 대기만으로는 다중 탭 부하 상황에서 실제 응답이 아직 안 왔을 수 있어, 네트워크 요청이 잠잠해질 때까지 기다린 뒤 결과를 읽도록 변경.

### 1.3.2

탭 여러 개가 동시에 페이지 이동할 때 일부가 "연결 안됨"으로 실패하던 문제 수정. 기본 goto()는 이미지/폰트 등 모든 리소스가 끝날 때까지 기다리는데, 탭이 한꺼번에 요청하면 일부가 30초 타임아웃으로 완전히 실패함을 실측 확인 — domcontentloaded 기준으로 변경.

### 1.3.1

동시 확인 클릭 시 낡은(stale) 사전 체크 값 때문에 일부 탭이 클릭 시도조차 안 되던 치명적 버그 수정 (사전 체크는 참고용으로만 쓰고, 실제 클릭은 항상 모든 탭에서 시도하도록 변경)

### 1.3.0

붙여넣기 자동 줄바꿈 및 우클릭 메뉴, 결과 오분류(페이지 오류를 무효 코드로 잘못 판정) 버그 수정

### 1.2.0

결과 애매 시 자동 재시도, 실패 코드만 재등록 버튼, 코드 옆 성공/실패 표시 추가

### 1.1.0

등록 확인 2단계(상품명/계정 확인 화면) 대응, 수동 동시 확인 트리거 버튼 추가

### 1.0.0

최초 정상 동작 버전 (등록 URL 오류, 이벤트 루프 정책 오류로 인한 브라우저 실행 실패, 탭 재이동 로직 오류, 완료 후 브라우저 자동 종료 등 치명적 버그 수정)

